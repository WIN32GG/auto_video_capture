# Auto camera capture

Capture images at a set interval and save them, upload them to the cloud.

## Installation

Install requirements:

```
pip install -r requirements.txt
```

## Usage

`python3 main.py -c /dev/video0 /dev/video1 --output /path/to/save/images`